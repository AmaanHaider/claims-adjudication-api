import { describe, it, expect } from "vitest";

describe.skip("adjudicationService (unit)", () => {
  it.skip("approves covered primary care at 100%", () => {
    expect(true).toBe(true);
  });

  it.skip("denies dental because it is not covered", () => {
    expect(true).toBe(true);
  });
});
