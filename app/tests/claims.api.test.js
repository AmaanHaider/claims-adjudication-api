import { describe, it, expect } from "vitest";

describe.skip("Claims API (integration)", () => {
  it.skip("GET /health returns ok", () => {
    expect(true).toBe(true);
  });

  it.skip("POST /claims creates claim and decisions (later step)", () => {
    expect(true).toBe(true);
  });
});
